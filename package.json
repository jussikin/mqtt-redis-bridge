{
  "name": "mqtt-redis-bridge",
  "version": "1.0.0",
  "description": "MQTT to Redis bridge that stores MQTT messages in Redis hashes",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mqtt",
    "redis",
    "bridge",
    "iot",
    "messaging"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.3",
    "mqtt": "^4.3.7",
    "redis": "^4.6.4"
  },
  "devDependencies": {
    "@types/mqtt": "^2.5.0",
    "@types/node": "^18.11.18",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
